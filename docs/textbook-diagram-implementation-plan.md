# 図解で学ぶ教科書 図解先行実装計画書

作成日: 2026-06-14

## 1. 目的

この計画書は、「図解で学ぶ教科書」の各章について、本文説明文より先に図解と動く図解を実装するための実装計画である。

今回の実装フェーズでは、説明文の完成を目的にしない。先に、各章で読者が見るべき構成図、ヘッダ図、シーケンス図、状態遷移図、パケットキャプチャ風の動く図を作る。

実装時は、この計画書に実装メモを追記し、その追記を含めて commit / push する。

## 2. 前提文書

図解の仕様は、次の設計書を正とする。

- `docs/textbook-curriculum-architecture.md`
- `docs/textbook-chapter-detailed-design.md`
- `docs/textbook-chapters-03-20-detailed-design.md`
- `docs/textbook-diagram-detail-matrix.md`

実装中に既存コードと設計書が矛盾する場合は、既存コードではなく設計書を優先する。特に、現行の `src/data/textbookChapters.ts` の第2章以降の章順は、全体アーキテクチャの20章体系に合わせて整理する。

## 3. 今回フェーズの範囲

### 3.1 実装するもの

- 第1章から第20章までの章ページを、図解先行の状態で整備する
- 各章に、最低1つ以上の動く図解を置く
- 原則として各節に図解を置く
- 図解作成済みの章は、ページ上部に「工事中」と表示する
- 図解内には、理解に必要なラベル、凡例、ステップ名、観測点、ヘッダ表示、短い補助文を置く
- 図解実装後は、この計画書の「実装メモ」に対象章、追加した図、検証結果、commit subject を記録してから push する

### 3.2 今回フェーズで実装しないもの

- 完成版の本文説明文
- 暗記用のまとめ本文
- 章末テスト、確認問題、学習進捗
- ノートモードや他コンテンツとの連携
- 外部画像ファイルの追加

ただし、図解を成立させるための最小限のラベル、ステップ説明、操作ボタン名、キャプチャ欄の項目名は実装対象に含める。

## 4. 実装方針

### 4.1 章状態

図解先行フェーズ用に、章の状態を次の3段階で扱う。

| 状態 | 意味 | ページ表示 |
|---|---|---|
| `draft` | 未実装。章一覧には準備中として出す | 本文ページは準備中 |
| `diagram` | 図解だけ先行実装済み。説明文は未完成 | 本文ページを表示し、上部に工事中を出す |
| `published` | 本文、図解、レビューが完了 | 通常表示 |

現行コードの `published` / `draft` だけでは、図解先行公開と本文完成を分けられない。最初の実装作業で `diagram` 相当の状態を追加する。

工事中表示の文言案:

> 工事中: この章は図解部分のみ先行公開中です。説明文は後続工程で追加します。

### 4.2 コンテンツ分離

教科書モードの変更は、次の範囲に閉じる。

- `src/pages/Textbook.tsx`
- `src/pages/TextbookChapter.tsx`
- `src/data/textbook*.ts`
- `src/components/textbook/*`
- `docs/textbook-*.md`

ホームとサイドバーの入口リンク以外には影響を出さない。既存のノートモード、午後問題演習、コラム、同期機能とは連携しない。

### 4.3 図解コンポーネント

既存の図解コンポーネントを再利用しつつ、足りない型は教科書モード内に追加する。

- 午後問題風ネットワーク構成図
- 動くシーケンス図
- 動くヘッダスタック図
- パケットキャプチャ風テーブル
- 物理構成と論理構成の比較図
- 状態遷移図
- 正常時/障害時/変更前後の比較図

図解はSVGまたはHTML/CSSで実装し、画像ファイルは追加しない。

### 4.4 動く図解の最低条件

動く図解は、点が線上を動くだけでは完了扱いにしない。各ステップで次を表示する。

- 今どの装置が処理しているか
- その装置がどのヘッダ、状態、設定、ログを見ているか
- 宛先、経路、許可、状態変化のどれを判断したか
- パケットキャプチャ風に見た場合、L2/L3/L4/L7のどこが変わるか

操作は、前へ、再生/停止、次へ、最初からを持たせる。操作ボタンは説明文量で上下に揺れない固定配置にする。

### 4.5 増補図解の扱い

各節に置く基本図解を先に実装し、その後のレビューや要望に応じて増補図解を追加する。増補候補は `docs/textbook-diagram-detail-matrix.md` の「増補図解候補」を参照する。

増補図解は、既存図へ情報を詰め込むためではなく、次の目的で追加する。

- 基本図解だけでは誤解が残る箇所を別視点で見せる
- 正常時、障害時、変更前後を比較する
- パケットキャプチャ、ログ、設定表など午後問題で出る別資料を重ねる
- 実務で見る観測点を補う

実装メモには、対象図解が「基本図解」か「増補図解」かを記録する。

## 5. 実装順

### 5.1 フェーズ0: 図解先行公開の基盤

- 章ステータスに `diagram` 相当を追加する
- 章ページ上部に工事中バナーを出せるようにする
- 20章体系に合わせて章データの骨組みを整理する
- 図解だけの章でもページを表示できるようにする
- 章一覧で「図解先行公開」「準備中」「公開中」を区別できるようにする

### 5.2 フェーズ1: 土台章

第1章から第4章を先に整える。ここで作る図解部品を後続章で再利用する。

| 章 | 図解の中心 | 必須の動き |
|---|---|---|
| 1. OSI参照モデルと通信の全体像 | OSI 7層、カプセル化、デカプセル化、再カプセル化、構成図 | データにL4、L3、L2が付く。ルータでL2を付け替える。サーバで外す |
| 2. TCP/UDPとポート番号 | ポート番号、5タプル、TCP/UDP比較 | 3ウェイハンドシェイクと5タプルの変化 |
| 3. DNS・DHCP・ARP | 名前解決、DORA、ARP範囲、TTL | DNS問い合わせ、DHCP DORA、ARP要求/応答 |
| 4. Web通信・TLS・HTTP | DNSからHTTPまでの時系列、TLS、HTTP/2 | DNS、TCP、TLS、HTTPが順に進む |

### 5.3 フェーズ2: 企業ネットワークの骨格

| 章 | 図解の中心 | 必須の動き |
|---|---|---|
| 5. L2スイッチング・VLAN・STP | 物理/論理構成、MAC学習、VLAN、STP | MAC学習、VLAN間ルーティング、STP/RSTP状態変化 |
| 6. ルーティング基礎・OSPF | 経路表、ロンゲストマッチ、OSPF | OSPF隣接、LSA交換、障害時収束 |
| 7. BGP・インターネット接続 | AS、eBGP/iBGP、経路広報、CDN | 経路広報、主回線障害時の経路切替 |
| 8. 冗長化・可用性・更改作業 | VRRP、状態同期、作業手順、切戻し | VRRP切替、状態同期あり/なしの通信継続差分 |

### 5.4 フェーズ3: Web系サービス経路

| 章 | 図解の中心 | 必須の動き |
|---|---|---|
| 9. ロードバランサ・プロキシ・CDN | VIP、リアルサーバ、CONNECT、CDN | LB振分け、CONNECTトンネル、CDN名前解決 |
| 10. VPN・WAN・SD-WAN | 拠点間接続、IPsec、NAT-T、SD-WAN | IKEからESPへの遷移、IPsecトンネルカプセル化 |

### 5.5 フェーズ4: セキュリティと認証

| 章 | 図解の中心 | 必須の動き |
|---|---|---|
| 11. セキュリティ境界・FW・SWG | 内部/DMZ/外部境界、FWルール、NAT、SWG | FW許可判定、NAT変換、SWG経由通信 |
| 12. 認証・認可・SSO・PKI | 証明書チェーン、SAML、Kerberos、802.1X | SAML、Kerberos、EAP-TLSの認証シーケンス |

### 5.6 フェーズ5: 応用領域

| 章 | 図解の中心 | 必須の動き |
|---|---|---|
| 13. 無線LAN | AP/WLC、チャネル、認証、ローミング | EAP-TLS、VLAN割当、ローミング |
| 14. IPv6 | IPv6アドレス、NDP、RA/SLAAC、A/AAAA | NDP、DAD、RA/SLAAC |
| 15. メール | SMTP配送、MX、SPF、DKIM、DMARC | SMTP配送、SPF/DKIM/DMARC判定 |
| 16. 仮想化・クラウド・SDN/VXLAN/EVPN | 物理/仮想、制御/転送、VXLAN | VXLANカプセル化、EVPNでMAC/IP到達性を配る |
| 17. VoIP・QoS・マルチキャスト | SIP/RTP、音声VLAN、QoS、配信木 | SIP呼制御、RTP、QoSマーキング、マルチキャストJoin |
| 18. IoT・LPWA・CoAP | IoT構成、LPWA、CoAP、DTLS | CoAP Confirmable/ACK、DTLS、再送 |

### 5.7 フェーズ6: 統合章

| 章 | 図解の中心 | 必須の動き |
|---|---|---|
| 19. 運用監視・障害切分け | ping、syslog、SNMP、監視点、切分け | 障害発生から観測点を順に確認する |
| 20. 午後問題総合読解 | 複合構成図、条件、設問根拠、答案化 | 構成図から通信経路、許可条件、障害原因を順に拾う |

## 6. 品質ゲート

### 6.1 技術正確性

- 各リンク上ではL2フレームとして運ばれ、その中にIPパケットやTCP/UDPセグメントが入ることを崩さない
- L2SW、ルータ、FW、LB、プロキシ、DNS、認証基盤が見る情報を混同しない
- 物理構成、論理構成、制御プレーン、データプレーンを図上で混ぜない
- VLAN、DMZ、NAT、FW、証明書、BGP、VRRPなどを必要な章でだけ登場させ、唐突に出さない

### 6.2 表示品質

- 線、矢印、ラベル、機器名が重ならない
- バッジや吹き出しで文字を隠さない
- 320px、768px、PC幅で破綻しない
- 図の操作ボタンは説明文量で位置が大きく動かない
- 黒一色の細かい図にせず、境界、経路、状態、ヘッダを控えめに色分けする

### 6.3 レビュー観点

実装単位ごとに、次の3者の観点でセルフレビューする。

- 初学者目線: どこを見ればよいか分かるか
- ネットワークエンジニア目線: ヘッダ、経路、状態、ログの扱いが正しいか
- IPA試験作成者目線: 構成図、条件、設問根拠を読む訓練になっているか

## 7. 検証手順

各実装単位で、少なくとも次を行う。

1. `npm run build`
2. 対象章をブラウザで確認
3. PC幅、タブレット幅、スマホ幅でスクリーンショット確認
4. 動く図解の前へ、再生/停止、次へ、最初からを確認
5. 線と文字の重なり、不要な線、ラベルの欠けを確認
6. 技術正確性、話の流れ、日本語ラベルをセルフレビュー

## 8. push と実装メモの運用

図解を実装したら、push 前に必ずこの計画書の「9. 実装メモ」へ行を追加する。

行には次を記録する。

- 日付
- 対象章
- 実装した図解
- 実装した動き
- 工事中表示の有無
- 検証結果
- commit subject
- 残課題

commit hash は、その commit 自身の中へ正確に書き込むことができないため、計画書には commit subject を記録し、最終報告で実際の commit hash と push 結果を報告する。

## 9. 実装メモ

| 日付 | 対象章 | 実装した図解 | 実装した動き | 工事中表示 | 検証結果 | commit subject | 残課題 |
|---|---|---|---|---|---|---|---|
| 2026-06-14 | 全章 | 図解先行実装の計画書を作成 | なし | 未実装 | docsのみ。コード動作への影響なし | Add textbook diagram implementation plan | 次回からフェーズ0に着手 |
| 2026-06-17 | 第1章〜第20章 | 基本図解として、各章に構成図、観測情報図、動く手順図を追加 | 各章に午後問題風構成図のステップ移動と、操作可能な動く手順図を追加 | 20章すべて表示済み | `npm run build` 成功。対象ファイル限定の `npx eslint ...` 成功。Playwrightで20章巡回し、各章3セクション、工事中表示、exam-network 1件、packet-flow 1件、コンソールエラーなしを確認。PC幅/スマホ幅で第1章・第20章を確認 | Add diagram-only textbook chapters | 本文説明文、増補図解、章別の精密レビューは後続工程 |

| 2026-06-17 | 全章共通 | 図の表示品質を改善。構成図のノード幅、ゾーン余白、線とラベルの重なり、動くパケットの走行位置を調整 | 構成図はパケット用レーンとリンクラベル用レーンを分離。動く手順図は矢印、広い描画領域、ノード補足、停止可能な再生操作を追加 | 既存の工事中表示を維持 | `npm run build` 成功。対象ファイル限定の `npx eslint ...` 成功。Playwrightで第1章、第5章、第10章、第20章を確認し、工事中表示、exam-network 1件、packet-flow 1件、コンソールエラーなしを確認。スマホ幅では動く図が720pxの描画幅を保ち横スクロールできることを確認 | Improve textbook diagram quality | 章固有の専門図や増補候補は後続工程で継続 |
| 2026-06-20 | 第1章〜第2章 | スマホ幅でもネットワーク構成図として読めるよう、縦リスト化をやめ、構成図の領域、機器、リンク、ラベルを保った縮小SVGへ調整 | 動く手順図もスマホ専用のミニ構成図へ変更し、パケットラベルとアクティブリンクを同じ図上で表示 | 既存の工事中表示を維持 | `npm run build` 成功。対象ファイル限定の `npx eslint ...` 成功。Playwrightで320px/390pxの第1章・第2章を確認し、`documentScrollWidth` と `bodyScrollWidth` が viewport 幅と一致すること、スマホ用構成図とスマホ用動く図が表示されることを確認。第3章は対象外として従来表示を維持 | Improve mobile textbook network diagrams | 第3章以降のスマホ最適化は後続工程 |
| 2026-06-20 | 第1章〜第2章 | スマホ用構成図の上下余白を削減し、領域、リンクラベル、ノードを縦方向に詰め直した | ステップ説明カードのラベルを右側へ配置し、キャプチャ表のL2/L3値を送信元と宛先で改行 | 既存の工事中表示を維持 | `npm run build` 成功。対象ファイル限定の `npx eslint ...` 成功。Playwrightで第1章のステップ3を320px/390pxで確認し、横スクロールなし、構成図の余白削減、キャプチャ表の改行を確認 | Compact mobile textbook diagrams | 第3章以降のスマホ最適化は後続工程 |
| 2026-06-20 | 第1章〜第20章 | これまでの図解指摘を全章へ反映。全章の構成図をスマホ/タブレットでは縮小SVGで表示し、長い機器名を自然改行、古い縦リスト図と横スクロール表をスマホ向け表示へ置換 | 全章の動く手順図をスマホ/タブレットではミニ構成図で表示。5ノード章はカード幅と高さを圧縮し、空きスペースと重なりを抑制 | 既存の工事中表示を維持 | `npm run build` 成功。対象ファイル限定の `npx eslint ...` 成功。Playwrightで20章を390px/768px/1280pxで巡回し、60ケースで横スクロールなし、表示切替、コンソールエラーなしを確認 | Apply diagram quality fixes to all chapters | 章固有の専門図追加と本文説明は後続工程 |
| 2026-06-21 | 第1章〜第20章 | 増補図解として「図解4: 読解の足場」を全章に追加。構成図の経路、判断材料、観測点、答案化の根拠を1枚にまとめ、午後問題で何を見るかを整理できるようにした | SVG上に巡回マーカーを追加し、構成図上の読み取り順を動きで追えるようにした。既存の動く手順図も維持 | 既存の工事中表示を維持 | `npm run build` 成功。対象ファイル限定の `npx eslint ...` 成功。Playwrightで20章を390px/768px/1280pxで巡回し、60ケースで横スクロールなし、各章4セクション、新規図解1件、パケット/フレーム図1件、動くマーカー2件を確認。スマホ幅の代表章をスクリーンショットで目視確認 | Add diagnostic map diagrams to textbook chapters | 本文説明と章固有の追加専門図は後続工程 |
| 2026-06-21 | 第1章〜第20章 | 「図解4: 読解の足場」のスマホ表示品質を改善。横長SVGを縮小表示する方式をやめ、スマホ専用SVGでノード、リンク、リンクラベルを大きく再配置した。4ノード章は横一列、5ノード以上は折り返し構成で表示 | スマホ専用SVGにも巡回マーカーを維持し、表示中のSVGだけで動くマーカー2件が確認できるようにした | 既存の工事中表示を維持 | `npm run build` 成功。対象ファイル限定の `npx eslint ...` 成功。Playwrightで20章を390px/768px/1280pxで巡回し、60ケースで横スクロールなし、新規図解1件、表示中SVG1件、表示中の動くマーカー2件を確認。第3章と第16章のスマホ表示をスクリーンショットで目視確認 | Improve diagnostic map mobile readability | 章固有の専門図追加と本文説明は後続工程 |
| 2026-06-21 | 第1章〜第20章 | 全図の技術正確性を監査。DNS/DHCP/ARP、SSO、メール、VoIPは直列経路ではなく分岐トポロジとして表現し、IPv6 RAの向き、WebのHTTPメソッドとポート、運用監視のHTTPステータスとポートの混同を修正した | 構成図、動く手順図、読解の足場で同じリンク定義を使うようにし、分岐章でも存在するリンク上をハイライト・巡回するようにした | 既存の工事中表示を維持 | `npm run build` 成功。対象ファイル限定の `npx eslint ...` 成功。データ整合性スクリプトで20章のリンク端点、ステップ到達性、非ポート値混入なしを確認。Playwrightで20章を390px/768px/1280pxで巡回し、60ケースで横スクロールなし、必要図解の表示を確認。第3章のスマホ表示をスクリーンショットで目視確認 | Audit textbook diagrams for technical correctness | 章固有の専門図追加と本文説明は後続工程 |
| 2026-06-21 | 引き継ぎ資料 | Claude Codeへ引き継ぐため、現在の進捗、今後の作業、裁量、制約、品質基準、開始プロンプトを設計書・計画書・専用引き継ぎ文書へ反映 | 図解・画面の動作変更なし | 既存の工事中表示を維持 | `npm run build` 成功。`git diff --check` 成功。`npm run lint` は既存の一時Chromeプロファイル配下と既存TSXのlintエラーで失敗したため、今回変更では未解消 | Document Claude Code textbook handoff | 引き継ぎ後は第1章から本文完成候補を作る |
| 2026-06-21 | 第1章 | 本文付き完成版（`textbookChapterOne.ts`）を採用。図解先行で空だった生成版（`makeChapter`）を置換し、OSI 7層・カプセル化・フレーム/パケット/セグメント・MAC/IP/ポート比較表・構成図・ARP比較・動く構成図・VLAN・午後読解の8節を、本文/コールアウト/examFocus/practicalFocus/pitfalls/summary 付きで配線。`textbookChapters.ts` で order===1 を完成版へ差し替え。id を `osi-communication`、タイトルを「OSI参照モデルと通信の全体像」に整合 | 既存の exam-network 動く図解（ARP解決・HTTPS経路）を本文と接続して利用。新規アニメーションの追加はなし | 工事中バナーを解除（status を `diagram`→`published`） | `npm run build` 成功。`npm run lint` は変更2ファイルで指摘なし（他ファイルの既存lintは未変更）。DOM計測で 375px 時 `maxScrollX=0`（横スクロールなし）・未クリップのはみ出し要素なし、本文8節/約10KB描画、章一覧が「19図解先行 / 1完成版 / 20全章」表示を確認。スクリーンショットは dev サーバ（HMR/PWA）で常時 network-busy のためタイムアウトし、DOM計測で代替 | feat(textbook): 第1章を本文付き完成版として公開 | スクリーンショットでの図解重なり目視は未実施（要 `vite preview` 等での確認）。第2章以降は引き続き本文未完成 |
| 2026-06-21 | 第1章 | 公開した第1章をユーザーがNG（exam-network図がスマホで横スクロール必須）。配線を解除し図解先行（工事中）表示へ戻した。教科書モードは要件定義からやり直す方針に変更 | 動作変更なし（配線解除のみ） | 工事中表示へ復帰 | `npm run build` 成功 | revert(textbook): 第1章をNGのため取り下げ | スマホで横スクロールしない図解・動くパケット図を前提に、要件定義→基本設計を作り直す。新方針: 1図+説明を極力1画面に収め縦横スクロールを最小化 |

## 10. 章別進捗

| 章 | 状態 | メモ |
|---|---|---|
| 1. OSI参照モデルと通信の全体像 | 図解先行（再設計中） | 一度 published にしたがスマホ横スクロールNGで取り下げ。要件定義からやり直し中 |
| 2. TCP/UDPとポート番号 | 図解先行実装済み | 5タプル、TCP/UDP、ポート番号、動く手順を追加。本文説明文は未実装 |
| 3. DNS・DHCP・ARP | 図解先行実装済み | DNS、DHCP、ARPの準備通信を図解化。本文説明文は未実装 |
| 4. Web通信・TLS・HTTP | 図解先行実装済み | DNS/TCP/TLS/HTTPの流れを図解化。本文説明文は未実装 |
| 5. L2スイッチング・VLAN・STP | 図解先行実装済み | MAC学習、VLAN間通信、STP観点を図解化。本文説明文は未実装 |
| 6. ルーティング基礎・OSPF | 図解先行実装済み | 経路表、OSPF隣接、LSA、転送を図解化。本文説明文は未実装 |
| 7. BGP・インターネット接続 | 図解先行実装済み | AS、経路広告、外部到達性を図解化。本文説明文は未実装 |
| 8. 冗長化・可用性・更改作業 | 図解先行実装済み | VRRP、切替、状態同期の観点を図解化。本文説明文は未実装 |
| 9. ロードバランサ・プロキシ・CDN | 図解先行実装済み | LB、Proxy/CDN、Originへの流れを図解化。本文説明文は未実装 |
| 10. VPN・WAN・SD-WAN | 図解先行実装済み | IPsec、underlay/overlay、NAT-T観点を図解化。本文説明文は未実装 |
| 11. セキュリティ境界・FW・SWG | 図解先行実装済み | 境界、許可条件、DMZ、SWG観点を図解化。本文説明文は未実装 |
| 12. 認証・認可・SSO・PKI | 図解先行実装済み | SP、IdP、アサーション、証明書観点を図解化。本文説明文は未実装 |
| 13. 無線LAN | 図解先行実装済み | AP、WLC、RADIUS、VLAN割当を図解化。本文説明文は未実装 |
| 14. IPv6 | 図解先行実装済み | NDP、DAD、RA/SLAAC、AAAA観点を図解化。本文説明文は未実装 |
| 15. メール | 図解先行実装済み | SMTP配送、MX、SPF/DKIM/DMARC観点を図解化。本文説明文は未実装 |
| 16. 仮想化・クラウド・SDN/VXLAN/EVPN | 図解先行実装済み | VTEP、underlay/overlay、VXLANを図解化。本文説明文は未実装 |
| 17. VoIP・QoS・マルチキャスト | 図解先行実装済み | SIP、RTP、QoS、配信観点を図解化。本文説明文は未実装 |
| 18. IoT・LPWA・CoAP | 図解先行実装済み | LPWA、CoAP/DTLS、ACK/再送観点を図解化。本文説明文は未実装 |
| 19. 運用監視・障害切分け | 図解先行実装済み | ICMP、SNMP/syslog、サービス監視を図解化。本文説明文は未実装 |
| 20. 午後問題総合読解 | 図解先行実装済み | 構成図、条件、観測点、答案化の動線を図解化。本文説明文は未実装 |

## 11. Claude Code引き継ぎ後の進め方

### 11.1 現在の到達点

2026-06-21時点で、第1章から第20章までの図解先行実装は一通り完了している。全章でページ表示、工事中表示、基本図解、動く手順図、読解の足場図が動作する。全図の技術監査では、直列経路にしてはいけないDNS/DHCP/ARP、SSO、メール、VoIPの構成を分岐トポロジへ修正し、IPv6 RAの向き、HTTPメソッドとポート、HTTPステータスとポートの混同も修正済みである。

ただし、現行状態は「教材として完成」ではない。図解を先に並べた試作基盤であり、本文説明、章固有の専門図、章ごとの教育導線、細かい日本語ラベル、午後問題との接続は後続工程で品質を上げる。

### 11.2 次にやるべきこと

次工程は、図を増やす前に章単位で教材化することである。章ごとに次の順で進める。

1. 対象章の詳細設計を確認する
2. 現行図解を、技術正確性、初学者の理解、午後問題読解の観点で再レビューする
3. 現行図解が不十分なら、既存図を直すか、増補図解として分割する
4. 図の直前または直後に、自然な日本語の本文説明を追加する
5. 重要語、混同しやすい語、午後問題で問われる判断材料を強調する
6. PC、タブレット、スマホ幅で図と本文を確認する
7. `npm run build`、`npm run lint`、対象画面のブラウザ確認を行う
8. この計画書の実装メモへ追記し、commit / push する

優先順位は、まず第1章から第4章である。理由は、第1章から第4章が全章の読み方、ヘッダ、名前解決、Web通信の土台になるためである。第5章以降は、本文化の前に第1章から第4章で確立した本文トーン、図解説明、スマホ表示の型を流用する。

### 11.3 裁量

引き継ぎ担当者は、次を行ってよい。

- 教科書モード内の本文、図解データ、図解コンポーネントを修正する
- 既存図が不適切なら、過去の実装にこだわらず削除または作り直す
- 1枚の図が混みすぎる場合、複数の図に分ける
- 章固有の専門図、パケットキャプチャ風表、ログ風表、設定表、変更前後図を追加する
- 章ごとの本文実装に必要な型を、`src/data/textbookChapters.ts` と `src/components/textbook/*` に追加する
- 技術的に迷う箇所は、IPA公式資料、RFC、主要ベンダドキュメントなど一次情報で確認する

ただし、次は行わない。

- ノートモード、クイズ、午後問題演習、同期、XP、勲章、活動履歴との連携追加
- ホームとサイドバーの入口導線以外の既存画面変更
- 画像ファイルの追加
- 図解だけを増やし、本文で意味を説明しない実装
- 技術的な自信がない状態で、推測のまま図や説明を確定すること

### 11.4 品質基準

顧客提出前の最低基準は次のとおり。

- 図の線、矢印、ラベル、バッジ、機器名が重ならない
- スマホ幅で横スクロールさせず、ネットワーク構成図として読める
- L2、L3、L4、L7、制御プレーン、データプレーンを混同しない
- 各リンク上ではL2フレームとして運ばれ、その中にIPパケットやTCP/UDPセグメントが入るという前提を崩さない
- ルータ、L3SW、FW、LB、プロキシ、DNS、認証基盤、監視基盤が見る情報を混同しない
- 用語が本文で登場する前に、図や説明で唐突に使われていない
- 日本語が自然で、初学者が読んでいてつまずきにくい
- 図の直後に「この図で見るところ」が分かる説明がある
- ネスペ午後問題で問われる、宛先判断、経路判断、許可条件、障害原因、変更影響、答案化に接続している

### 11.5 引き継ぎプロンプト

Claude Codeへ引き継ぐ場合は、`docs/textbook-claude-code-handoff.md` を最初に読ませる。特に、同文書の「8. Claude Code向け開始プロンプト」をそのまま開始プロンプトとして使う。
